export enum ECookieArg {
  USER = "token",
  SESSION_ID = "session-id",
  OAUTH_FLOW = "oauth-flow",
  SOURCE = "source",
  CONFIRM_CODE = "confirm-code",
  EXPIRED_MODAL_SHOWN = "trial-expired-modal-shown",
}
